import { DownloadTableToCsvDirective } from './download-table-to-csv.directive';

describe('DownloadTableToCsvDirective', () => {
  it('should create an instance', () => {
    const directive = new DownloadTableToCsvDirective();
    expect(directive).toBeTruthy();
  });
});
